/** @type {import('next').NextConfig} */
const nextConfig = {
	output: "export",
	basePath: "/tools", // Substitua <nome-do-repo> pelo nome do seu repositório
	assetPrefix: "/tools/",
};

export default nextConfig;
